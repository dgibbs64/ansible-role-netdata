---
- name: "Converge"
  hosts: all
  gather_facts: false
  vars:
    netdata_claim_token: "{{ lookup('env', 'CLAIM_TOKEN') }}"
    netdata_claim_room_id: "{{ lookup('env', 'CLAIM_ROOM_ID') }}"
  roles:
    - role: "dgibbs64.netdata"
